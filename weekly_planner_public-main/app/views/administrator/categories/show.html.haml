%p{:style => "color: green"}= notice
= render @category
%div
  = link_to "Edit this category", edit_administrator_category_path(@category)
  |
  \#{link_to "Back to categories", administrator_categories_path}
  
  \#{button_to "Destroy this category", [:administrator, @category], method: :delete}
